#####################################################################################################################

- Dans main. Je suggere plutot une approche comme ça pour ne pas changer le format d entree des données du prof (pseudocode non testé) :
  NB : Si on change le format du fichier, la fonction acquisitionMultigraphe(nomFichier) est à modifier.

	print("Veuillez entrer les données du graphe. Saisir :
            \t 1 : acquisition par clavier
            \t 2 : acquisition par fichier")

    x = readint()  <---- en cas de mauvaise saisie? Mettre un try catch et/ou message d'erreur?

    if x == 1 :
        print("Acquisition par clavier.")
        mg = parseMultigraph()   <------ completer cette fonction avec les affichages à chaque etape precedente a la saisie
                                        Exemple :   "Inserer nb noeuds"
                                                    n = readint
                                                    "Inserer nb arcs"
                                                    m = readint
                                                    ...
    elif x == 2 :
        print("Acquisition par fichier. Inserer nom fichier")
        readstr
        mg = acquisitionMultigraphe(nomFichier) <------ j'ai ajouté déjà une verification su le nom de fichier saisi

    else :
        "Message erreur, mauvaise choix"

	if mg == None:
        "Message erreur"
		return

	print("Le graph a été parsé sans erreur.")

	print("Veuillez entrer le sommet de départ : ")
	x = readstr()
	print("Veuillez entrer le sommet d'arrivée : ")
	y = readstr()
	print("Veuillez entrer l'intervalle dans lequel le trajet est effectué : ")
	interval = readInterval() # tuple: (start, end)

	print("Graphe en cours de transformation...")
	g = mg.transform_to_graph()

	print("Calcul des chemins minimaux...")

#####################################################################################################################

- Prevoir si le choix du VERBOSE doit etre determinée au clavier par l'utilisateur (print "Afficher détails sur le deroulement des algos?")
ou sinon si on va le preciser à la main dans le programme.
En tout cas faire en sorte que la valeur du VERBOSE soit rentré une seule fois et se transmet à toutes les fonctions automatiquement.

#####################################################################################################################

- 	# TODO: Rajouter tous les tests nécessaires pour éviter les erreurs

#####################################################################################################################

- [Je suis pas sûre si il y a encore ce pb mais normalement non] Dans BFS : refleshir à un moyen pour verifier que la liste sommetsVisites soit correcte et ait visite tous les noeuds qu'elle devait
> C'est la partie en gris, normalement on doit verifier que les sommets travaillés ce sont bien tous, MAIS ce n est pas necessaire pour le fonctionnement

#####################################################################################################################

- Adapter les fonctions pour tracer les courbes.

#####################################################################################################################

- Trouver ou créer un générateur de grands graphes pour tester avec un nb d'arcs et de sommets plus grand ?

#####################################################################################################################